<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width">
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<title>{{ subject }}</title>
</head>
<body>

<p>
{{ (duns ~ 'email_recover_password.texts.welcome')|trans }}
</p>

<p>
{{ (duns ~ 'email_recover_password.texts.change_password')|trans }}<br/>
<a href="{{ url(recover_password_confirm_route, {token: token}) }}">{{ (duns ~ 'email_recover_password.labels.change_password_link')|trans }}</a>
</p>

<p>
{{ (duns ~ 'email_recover_password.texts.copy_link')|trans }}<br/>
<pre>{{ url(recover_password_confirm_route, {token: token}) }}</pre>
</p>

<p>
<a href="{{ url(login_route) }}">{{ (duns ~ 'email.labels.login_link')|trans }}</a>
</p>

</body>
</html>